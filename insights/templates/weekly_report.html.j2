<!DOCTYPE html>
<html lang="zh-Hant-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minecraft 模組週報 - {{ report.timestamp }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .platform-badge {
            font-size: 0.8em;
            padding: 0.3em 0.6em;
        }
        .modrinth { background-color: #00AF5C; color: white; }
        .hangar { background-color: #2D3283; color: white; }
        .polymart { background-color: #FF6B6B; color: white; }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Minecraft 模組週報</h1>
        <p class="text-muted">更新時間：{{ report.timestamp }}</p>

        <!-- 本週新星 -->
        <div class="card mb-4">
            <div class="card-header">
                <h2 class="h5 mb-0">🌟 本週新星</h2>
            </div>
            <div class="card-body">
                <div class="row">
                    {% for resource in report.trending.rising_stars %}
                    <div class="col-md-6 mb-3">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <a href="{{ resource.website_url }}" target="_blank">{{ resource.name }}</a>
                                    <span class="badge platform-badge {{ resource.platform.lower() }}">
                                        {{ resource.platform }}
                                    </span>
                                </h5>
                                <p class="card-text">{{ resource.description }}</p>
                                <p class="card-text">
                                    <small class="text-muted">
                                        作者：{{ resource.author }} |
                                        下載次數：{{ "{:,}".format(resource.downloads) }}
                                    </small>
                                </p>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- 版本更新趨勢 -->
        <div class="card mb-4">
            <div class="card-header">
                <h2 class="h5 mb-0">📊 版本支援趨勢</h2>
            </div>
            <div class="card-body">
                <h3 class="h6">熱門版本 Top 5</h3>
                <ul class="list-group">
                    {% for version, stats in report.version_updates.popular_versions %}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        {{ version }}
                        <span class="badge bg-primary rounded-pill">
                            {{ stats.count }} 個資源
                        </span>
                    </li>
                    {% endfor %}
                </ul>
            </div>
        </div>

        <!-- 分類焦點 -->
        <div class="card mb-4">
            <div class="card-header">
                <h2 class="h5 mb-0">📦 分類焦點</h2>
            </div>
            <div class="card-body">
                {% for category, highlights in report.category_highlights.items() %}
                <div class="mb-4">
                    <h3 class="h6">{{ category }}</h3>
                    <p>
                        總資源數：{{ highlights.total_resources }} |
                        總下載次數：{{ "{:,}".format(highlights.total_downloads) }}
                    </p>
                    <div class="list-group">
                        {% for resource in highlights.top_resources %}
                        <a href="{{ resource.website_url }}" class="list-group-item list-group-item-action">
                            <div class="d-flex w-100 justify-content-between">
                                <h5 class="mb-1">{{ resource.name }}</h5>
                                <span class="badge platform-badge {{ resource.platform.lower() }}">
                                    {{ resource.platform }}
                                </span>
                            </div>
                            <p class="mb-1">{{ resource.description }}</p>
                            <small class="text-muted">
                                作者：{{ resource.author }} |
                                下載次數：{{ "{:,}".format(resource.downloads) }}
                            </small>
                        </a>
                        {% endfor %}
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>

        <!-- 平台統計 -->
        <div class="card mb-4">
            <div class="card-header">
                <h2 class="h5 mb-0">📈 平台統計</h2>
            </div>
            <div class="card-body">
                <div class="row">
                    {% for platform, stats in report.platform_stats.items() %}
                    <div class="col-md-4 mb-3">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">{{ platform }}</h5>
                                <p class="card-text">
                                    總資源數：{{ stats.total_resources }}<br>
                                    總下載次數：{{ "{:,}".format(stats.total_downloads) }}
                                </p>
                                <h6 class="card-subtitle mb-2 text-muted">資源類型分布</h6>
                                <ul class="list-unstyled">
                                    {% for type, count in stats.resource_types.items() %}
                                    <li>{{ type }}: {{ count }}</li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 